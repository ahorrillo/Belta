<IfModule mod_rewrite.c>
    RewriteEngine On

    # Si la petición ya viene con /public/, no hacemos nada
    RewriteCond %{REQUEST_URI} !^/public/

    # Redirigimos todo el tráfico a la carpeta public
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
